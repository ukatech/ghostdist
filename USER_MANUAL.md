# GhostDist 使用方法マニュアル

## クイックスタート

### 1. プロジェクトを作成する

「**新規作成**」ボタンを押して処理条件を作成します。

**おすすめ**: 「ネットワーク更新用」と「アーカイブアップ用」の2つ作ると便利です。1つ作ったあと「**コピー**」ボタンで複製すると楽です。

### 2. ネットワーク更新の設定

1. **処理タイプ**: 「ネットワーク更新ファイルアップロード」を選択
2. **基準フォルダ**: ゴーストのパス（`install.txt`や`readme.txt`がある場所）を指定
3. **FTP設定**: サーバー・ID・パスワードを入力（パッシブモード推奨）
4. **アップロード先ディレクトリ**: 更新ファイルを置きたい場所を入力
   - 例: `http://XXX.ne.jp/ghost_update/test/` にアップしたい場合 → `ghost_update/test/`

### 3. NAR作成+アップロードの設定

1. **処理タイプ**: 「ゴーストアーカイブ(NAR)作成/FTPアップロード」を選択
2. **アップロード先ディレクトリ**: NARファイルを置きたい場所を入力
   - 例: `http://XXX.ne.jp/ghost/` にアップしたい場合 → `ghost/`
3. **配布ページHTMLファイル**: 配布ページのHTMLファイルのフルパスを指定（任意）
   - HTML内の `%uploaddate` `%uploadtime` `%uploadsize` が自動置換されます
4. 他の設定はネットワーク更新と同じ

### 4. 実行する

実行したいプロジェクトにチェックを入れて「**実行**」ボタンを押します。処理が終わったらログを確認し、FTPソフトでファイルがアップされているかチェックしてください。

### Tips

- **起動時にチェック**: よく使うプロジェクトはこれを有効にしておくと便利
- **ルートにアップしたい場合**: サーバーによっては `/` ではなく `/public_html/` と書く必要があります
- **NARのバックアップ**: NARファイル名を `保存先\ゴースト名_%shortyear%%month%%day%.nar` のようにすると日付入りで保存できます

---

## プロジェクトの種類

1. **ネットワーク更新** - `updates2.dau`生成、変更ファイルのみFTPアップロード
2. **NAR作成+アップロード** - NAR作成、FTP/ななろだアップロード、HTML変数置換
3. **NAR作成のみ** - NARファイル作成のみ

---

## 共通アップロード設定

複数プロジェクトで同じFTP/ななろだサーバーを使う場合、プロジェクト設定画面で「**この設定を共通設定にする**」ボタンをクリックすると共通設定として保存されます。

---

## ファイルパターン

処理対象・除外パターンは1行1パターンで入力。空欄時はデフォルトパターンが適用されます。

**例（処理対象）**:
```
*.txt
*.dic
ghost/master/*.txt
shell/master/*.png
```

**例（除外）**:
```
*.bak
*.tmp
profile.txt
```

**注意**: サブディレクトリの再帰検索（`**`）は非対応です。

---

## HTML変数置換

**日時系（NARファイル名・HTML共通）**:
- `%year%` `%shortyear%` `%month%` `%day%` `%hour%` `%minute%` `%second%`

**アップロード情報（HTML内、Uploadモード時のみ）**:
- `%uploaddate%` - 日付（`yyyy/MM/dd`）
- `%uploadtime%` - 時刻（`HH:mm:ss`）
- `%uploadsize%` - ファイルサイズ（KB単位）

---

## ななろだアップロード

FTPサーバーを持たない場合、ななろだ（NarNaLoader）を利用できます。

設定項目:
- **Ghost ID**: ななろだで登録したゴーストID
- **アップロードURL**: ななろだのアップロードスクリプトURL
- **ユーザーID/パスワード**: ななろだのアカウント情報

**注意**: ななろだ使用時、HTMLファイルはローカルのみ更新されます（FTPアップロードなし）。

---

## Unicode対応

Shift_JISで表現できない文字（絵文字等）を含むファイル名は、Info-ZIP Unicode Path Extra Field（0x7075）が自動追加されます。

- **SSP 2.5.55以降**: 正しく表示
- **SSP 2.5.54以前**: `?`等に置換される可能性あり

---

## トラブルシューティング

### FTP接続失敗

| エラー | 対処 |
|--------|------|
| 530 Login failed | ユーザーID/パスワード確認 |
| タイムアウト | パッシブモード切替、ファイアウォール確認 |
| パスが見つからない | FTPサーバー上のディレクトリ存在確認 |

### ファイルがアップロードされない

1. 「**プレビュー**」ボタンで処理対象ファイルを確認
2. 除外パターンに該当していないか確認
3. ネットワーク更新: MD5が変わっていないファイルはスキップされます

### HTML変数が置換されない

- HTMLパスが正しいか確認
- 処理タイプが「NAR作成+アップロード」か確認

---

## FAQ

### ログファイルの保存場所

`log/log<YYYYMMDDHHMMSS>.log`（「ログファイルに保存」チェック時のみ）

### 対応していない機能

- SFTP/SSHFTP
- プロキシ接続
- 処理中のキャンセル
- Mac/Linux

---

## サポート

バグ報告・機能要望: [GitHub Issues](https://github.com/ukatech/ghostdist/issues)
