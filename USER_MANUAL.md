# GhostDist 使用方法マニュアル

GhostDistは、伺かゴーストの配布作業を自動化するツールです。このマニュアルでは、初心者の方でも使えるように、GUI操作を中心に詳しい使い方を説明します。

## 目次

1. [はじめに](#はじめに)
2. [インストール](#インストール)
3. [初期設定](#初期設定)
4. [プロジェクトの作成](#プロジェクトの作成)
5. [基本的な使い方](#基本的な使い方)
6. [各機能の詳細](#各機能の詳細)
7. [トラブルシューティング](#トラブルシューティング)
8. [FAQ](#faq)

---

## はじめに

### GhostDistでできること

GhostDistは、伺かゴーストの配布に必要な以下の作業を自動化します。

1. **ネットワーク更新**
   - `updates2.dau`ファイルの生成
   - 変更されたファイルのみを自動検出
   - FTPサーバーへの差分アップロード

2. **NAR作成+アップロード**
   - NAR（NARファイル）の作成
   - FTPサーバーへのアップロード、または**ななろだ**へのアップロード
   - 配布ページHTMLの自動更新（日時・ファイルサイズ変数置換）

3. **NAR作成のみ**
   - NAR（NARファイル）の作成のみ実行（アップロードなし）

### 動作環境

- **.NET Framework**: 4.8 以上

---

## インストール

### ステップ1: 配置

ダウンロードした`GhostDist.exe`を、使いやすい場所に配置します。

**推奨配置場所の例**:
```
C:\Tools\GhostDist\GhostDist.exe
```

### ステップ2: 起動確認

`GhostDist.exe`をダブルクリックして起動してください。
ウィンドウが開けば、インストール完了です。

**注意**: 初回起動時、Windows Defenderなどのセキュリティソフトが警告を出すことがあります。その場合は「詳細情報」→「実行」を選択してください。

---

## 初期設定

### 初回起動時

GhostDistを初めて起動すると、プロジェクトが何も登録されていない状態です。

**最初にやること**:
1. `GhostDist.exe`を起動
2. 「**新規作成**」ボタンをクリック
3. プロジェクト設定画面が開きます

### 共通FTP設定（オプション）

複数のゴーストで同じFTPサーバーを使う場合、共通FTP設定を先に登録しておくと便利です。

#### ステップ1: プロジェクト設定画面を開く

1. 「新規作成」ボタンをクリック
2. プロジェクト設定画面が開く

#### ステップ2: 共通FTP設定を入力

1. FTP/アップロード設定セクションで、サーバー情報を入力
   - **サーバー**: `ftp.example.com`
   - **ユーザーID**: `myusername`
   - **パスワード**: `mypassword`
   - ☑ **パッシブモード**（通常はチェック推奨）
   - ☐ **FTPS(SSL)**（サーバーがFTPS対応の場合のみチェック）

2. 「**設定として保存**」ボタンをクリック
   - これで共通FTP設定として保存されます

3. 「キャンセル」で一旦閉じる（プロジェクトは作成しなくてOK）

**注意**: 共通FTP設定は必須ではありません。各プロジェクトで個別に設定することもできます。

---

## プロジェクトの作成

### ネットワーク更新プロジェクトの作成

ゴーストのネットワーク更新を自動化するプロジェクトを作成します。

#### ステップ1: 新規作成

1. メイン画面で「**新規作成**」ボタンをクリック
2. プロジェクト設定画面が開きます

#### ステップ2: 基本設定

1. **名称**: プロジェクト名を入力（例: `マイゴースト - ネットワーク更新`）
2. **処理タイプ**: 「**ネットワーク更新ファイルアップロード**」を選択
3. **基準フォルダ**: ゴーストのルートフォルダを指定（「参照」ボタンで選択）
   - 例: `C:\ssp\ghost\myghost`

#### ステップ3: FTP設定

**共通FTP設定を使う場合**:
- ☑ **共通FTP設定を使用** にチェック
- **アップロード先ディレクトリ**: FTPサーバー上のパスを入力（例: `/public_html/ghost/`）

**個別にFTP設定をする場合**:
- ☐ **共通FTP設定を使用** のチェックを外す
- **サーバー**: `ftp.example.com`
- **ユーザーID**: `myusername`
- **パスワード**: `mypassword`
- ☑ **パッシブモード**
- ☐ **FTPS(SSL)**（必要に応じて）
- **アップロード先ディレクトリ**: `/public_html/ghost/`

#### ステップ4: ファイルフィルタ設定

**処理対象パターン**（1行1パターン）:
```
delete.txt
install.txt
ghost/master/*.txt
ghost/master/*.dic
ghost/master/*.dll
shell/master/*.png
shell/master/*.txt
```

**除外パターン**（1行1パターン）:
```
profile.txt
ghost/master/*.log
ghost/master/satori_savedata.txt
*.bak
*.tmp
```

**ヒント**:
- 空欄のままにすると、デフォルトパターンが自動適用されます
- 「**プレビュー**」ボタンで、実際に処理されるファイルを事前確認できます

#### ステップ5: その他の設定

- ☑ **起動時にチェック**: GhostDist起動時に自動的にチェックを入れる場合

#### ステップ6: 保存

「**OK**」ボタンをクリックして保存します。

---

### NAR作成+アップロードプロジェクトの作成

NARファイルを作成してFTPまたはななろだにアップロードするプロジェクトを作成します。

#### ステップ1〜3: 基本設定とFTP設定

ネットワーク更新プロジェクトと同じ手順ですが、以下の点が異なります。

**処理タイプ**: 「**ゴーストアーカイブ(NAR)作成/FTPアップロード**」を選択

#### ステップ4: アップロード方式の選択

**FTP直接アップロードの場合**:
- ○ **FTP直接アップロード** を選択
- FTP設定を入力（共通設定または個別設定）

**ななろだアップロードの場合**:
- ○ **ななろだアップロード** を選択
- **Ghost ID**: ななろだで登録したゴーストID（例: `myghost_unique_id`）
- **アップロードURL**: ななろだのアップロードスクリプトURL（例: `http://nanarloader.example.com/upload.php`）
- **ユーザーID**: ななろだのユーザー名
- **パスワード**: ななろだのパスワード

#### ステップ5: HTML/NAR設定

**HTMLファイル**: 配布ページのHTMLファイルパス（「参照」ボタンで選択）
   - 例: `C:\web\myghost\index.html`

**NAR/ZIPファイル名**: NARファイル名のテンプレート
   - 例: `myghost_%year%%month%%day%.nar`
   - 使用可能な変数:
     - `%year%`: 年（4桁）
     - `%shortyear%`: 年（2桁）
     - `%month%`: 月（2桁）
     - `%day%`: 日（2桁）
     - `%hour%`: 時（2桁）
     - `%minute%`: 分（2桁）
     - `%second%`: 秒（2桁）

#### ステップ6: ファイルフィルタ設定

ネットワーク更新と同様に設定します。空欄の場合はデフォルトパターンが適用されます。

#### ステップ7: 保存

「**OK**」ボタンをクリックして保存します。

---

### NAR作成のみプロジェクトの作成

FTPアップロードせず、NARファイルの作成のみを行うプロジェクトです。

**手順**: NAR作成+アップロードプロジェクトとほぼ同じですが、以下の点が異なります。

1. **処理タイプ**: 「**ゴーストアーカイブ(NAR)作成**」を選択
2. FTP設定は不要（グレーアウトされます）
3. HTMLファイル、NARファイル名のみ設定

**用途**:
- NARファイルを手動でアップロードしたい場合
- NARファイルをメール送付したい場合
- バックアップ用にNARを作成したい場合

---

## 基本的な使い方

### メイン画面の説明

GhostDistのメイン画面は以下のような構成になっています。

```
┌─────────────────────────────────────────────────────┐
│ GhostDist v0.2.1.4 改                                │
├─────────────────────────────────────────────────────┤
│ [プロジェクト一覧]                  [操作ボタン]      │
│ ☑ プロジェクト名1                  新規作成(&N)      │
│ ☐ プロジェクト名2                  編集(&E)          │
│ ☑ プロジェクト名3                  削除(&D)          │
│                                    コピー(&C)        │
│                                    ↑ ↓              │
│                                                      │
│ [実行設定]                                           │
│ ☑ ログファイルに保存                                 │
│ ☐ ログウィンドウ非表示                               │
│                                                      │
│ [フィルタボタン]                                     │
│ [ネットワーク] [NAR作成+UP] [NAR作成] [全選択] [全解除] │
│                                                      │
│                                   [実行(&R)] [閉じる] │
└─────────────────────────────────────────────────────┘
```

**主要ボタン・機能**:
- **新規作成**: 新しいプロジェクトを作成
- **編集**: 選択中のプロジェクトを編集（ダブルクリックでも可）
- **削除**: 選択中のプロジェクトを削除（確認ダイアログ表示）
- **コピー**: 選択中のプロジェクトを複製
- **↑↓**: プロジェクトの表示順序・実行順序を変更
- **ネットワーク**: ネットワーク更新プロジェクトのみ全選択
- **NAR作成+UP**: NAR作成+アップロードプロジェクトのみ全選択
- **NAR作成**: NAR作成のみプロジェクトのみ全選択
- **全選択**: すべてのプロジェクトを選択
- **全解除**: すべての選択を解除
- **実行**: チェックが入っているプロジェクトを上から順に実行
- **ログファイルに保存**: チェック時、`log/log<YYYYMMDDHHMMSS>.log`にログ保存
- **ログウィンドウ非表示**: チェック時、実行時のログウィンドウを表示しない

### プロジェクト設定画面の詳細

「編集」ボタンまたはダブルクリックでプロジェクト設定画面が開きます。

**主なセクション**:
1. **基本設定**: プロジェクト名、動作モード、基準フォルダ
2. **FTP/アップロード設定**: サーバー情報、認証情報
3. **ファイルフィルタ**: 処理対象パターン、除外パターン（1行1パターン）
4. **HTML/NAR設定**: HTMLファイルパス、NARファイル名テンプレート（Upload/NarCreateのみ）
5. **プレビュー**: 設定したパターンに一致するファイルを事前確認

**プレビューボタンの使い方**:
1. 処理対象パターンと除外パターンを設定
2. 「**プレビュー**」ボタンをクリック
3. 実際に処理されるファイルの一覧が表示される
4. 意図したファイルが含まれているか確認

---

### ネットワーク更新の実行

ゴーストのファイルを更新した後、ネットワーク更新を配信する手順です。

#### ステップ1: プロジェクトの選択

1. GhostDistを起動
2. 左側のリストから更新したいゴースト名の横にある**チェックボックスにチェック**を入れる
3. 複数のゴーストを同時に更新する場合は、複数にチェックを入れる

#### ステップ2: 実行

1. 右下の「**実行**」ボタンをクリック
2. ログウィンドウが開き、処理が開始されます
3. 以下の作業が自動で行われます：
   - ファイルのMD5ハッシュ計算
   - `updates2.dau`の生成
   - 前回との差分検出
   - 変更されたファイルのみFTPアップロード

#### ステップ3: 完了確認

ログウィンドウに「処理が完了しました」と表示されたら完了です。

---

### NAR作成+アップロードの実行

新しいバージョンのNARファイルを作成・配布する手順です。

#### ステップ1: HTMLファイルの準備

配布ページのHTMLに、以下の変数を埋め込んでおきます。

```html
<!DOCTYPE html>
<html>
<head>
    <title>マイゴースト 配布ページ</title>
</head>
<body>
    <h1>マイゴースト</h1>
    <p>最終更新日: %uploaddate%</p>
    <p>更新時刻: %uploadtime%</p>
    <p>ファイルサイズ: %uploadsize%</p>
    <p><a href="myghost_%year%%month%%day%.nar">ダウンロード</a></p>
</body>
</html>
```

**使用可能な変数**:

**日時系（NARファイル名・HTML共通）**:
- `%year%`: 年（4桁、例: `2026`）
- `%shortyear%`: 年（2桁、例: `26`）
- `%month%`: 月（2桁、ゼロ埋め、例: `01`）
- `%day%`: 日（2桁、ゼロ埋め、例: `14`）
- `%hour%`: 時（2桁、ゼロ埋め、例: `09`）
- `%minute%`: 分（2桁、ゼロ埋め、例: `30`）
- `%second%`: 秒（2桁、ゼロ埋め、例: `45`）

**アップロード情報（HTML内のみ、Uploadモード時のみ置換）**:
- `%uploaddate%`: アップロード日（`yyyy/MM/dd`形式、例: `2026/01/14`）
- `%uploadtime%`: アップロード時刻（`HH:mm:ss`形式、例: `09:30:45`）
- `%uploadsize%`: NARファイルサイズ（**KB単位**、例: `1234 KB`）

#### ステップ2: プロジェクトの選択

1. NAR作成+アップロードプロジェクトにチェックを入れる
2. 複数のNARを作成する場合は、複数にチェックを入れる

#### ステップ3: 実行

1. 「**実行**」ボタンをクリック
2. 処理が開始され、以下の作業が自動で行われます：
   - NARファイルの作成（`install.txt`、`updates2.dau`自動生成）
   - FTPまたはななろだへのNARアップロード
   - HTMLファイル内の変数置換
   - HTMLファイルのアップロード（FTP直接の場合のみ）

#### ステップ4: 完了確認

- ログに「NAR作成完了」「アップロード完了」と表示されているか
- FTPサーバーまたはななろだにNARファイルがアップロードされているか
- ブラウザで配布ページを開いて、日時やファイルサイズが正しく置換されているか

---

## 各機能の詳細

### Network（ネットワーク更新）

#### 動作の詳細

1. **ファイルスキャン**: 基準フォルダ以下のすべてのファイルをスキャン
2. **フィルタリング**: 処理対象パターンに一致し、除外パターンに一致しないファイルを抽出
3. **MD5計算**: 各ファイルのMD5ハッシュを計算
4. **updates2.dau生成**: 以下の形式でupdates2.dauを生成

```
filename\x01MD5hash\x01size=12345\x01
filename2\x01MD5hash2\x01size=67890\x01charset=Shift_JIS\x01
```

5. **差分検出**: 前回のupdates2.dauと比較し、変更されたファイルのみを抽出
6. **FTPアップロード**: 変更されたファイルとupdates2.dauをアップロード

#### 初回実行時の注意

初回実行時はサーバー上にupdates2.dauが存在しないため、すべてのファイルがアップロードされます。

#### ゴースト/シェル両方の更新

ゴーストとシェルの両方を同時に更新する場合は、プロジェクトを2つ作成します。

**例**:
- プロジェクト1: 「マイゴースト - ゴースト本体」（基準フォルダ: `C:\ssp\ghost\myghost\ghost`）
- プロジェクト2: 「マイゴースト - シェル」（基準フォルダ: `C:\ssp\ghost\myghost\shell\myshell`）

両方にチェックを入れて「実行」すれば、順番に処理されます。

---

### Upload（NAR作成+アップロード）

#### 動作の詳細

1. **ファイル収集**: 処理対象パターンに一致するファイルを収集
2. **install.txt生成**: ゴーストのメタ情報を自動生成（`descript.txt`から取得）
3. **updates2.dau生成**: MD5ハッシュ情報を生成
4. **NAR作成**: ZIPファイル形式で圧縮（拡張子.nar、Unicode対応）
5. **アップロード**: 以下のいずれかの方法でアップロード
   - **FTP直接アップロード**: FTPサーバーにNARを送信
   - **ななろだアップロード**: HTTP POSTでななろだサーバーに送信
6. **HTML変数置換**: HTMLファイル内の変数を実際の値に置換
7. **HTMLアップロード**: 更新されたHTMLをアップロード（FTP直接の場合のみ）

#### FTP直接アップロード vs ななろだアップロード

**FTP直接アップロード**:
- 自分のFTPサーバーに直接NARをアップロード
- HTMLファイルもFTPで自動アップロード
- 配布ページURLは自分で管理

**ななろだアップロード**:
- ななろだサーバーにHTTP POSTでNARを送信
- HTMLファイルは**ローカルのみ更新**（FTPアップロードなし）
- Ghost ID（ゴーストID）が必要
- アップロードURL（ななろだのアップロードスクリプトURL）が必要
- レスポンス: `<!--result: メッセージ-->`および`<!--resultcode: success/failure-->`

#### install.txtの内容

`install.txt`は不足している場合は自動生成され、以下のような内容になります。

```
type,ghost
name,ゴースト名
craftman,作者名
craftmanw,作者名（Unicode）
directory,ゴーストフォルダ名
```

**注意**: `descript.txt`の内容から自動的に情報を取得します。

#### NARファイルの構造

```
myghost_20260114.nar
├── install.txt
├── updates2.dau
└── ghost/
    ├── master/
    │   ├── descript.txt
    │   ├── updates2.dau
    │   └── ...（辞書ファイル等）
    └── ...
```

---

### NarCreate（NAR作成のみ）

#### 動作の詳細

`Upload`と同じですが、FTPアップロードを行わず、NARファイルの作成のみを実行します。

**用途**:
- NARファイルを手動でアップロードしたい場合
- NARファイルをメール送付したい場合
- 複数のNARを作成してまとめてアップロードしたい場合

---

## プロジェクトの管理

### プロジェクトの編集

1. メイン画面でプロジェクトを選択
2. 「**編集**」ボタンをクリック（またはダブルクリック）
3. プロジェクト設定画面が開きます
4. 設定を変更して「OK」で保存

### プロジェクトの削除

1. メイン画面でプロジェクトを選択
2. 「**削除**」ボタンをクリック
3. 確認ダイアログが表示されるので「はい」をクリック

### プロジェクトのコピー

既存のプロジェクトを複製して、設定を変更したい場合に便利です。

1. メイン画面でプロジェクトを選択
2. 「**コピー**」ボタンをクリック
3. コピーされたプロジェクトが作成される（名前に「のコピー」が追加されます）
4. 必要に応じて「編集」で設定を変更

### プロジェクトの順序変更

プロジェクトの実行順序を変更できます。

1. メイン画面でプロジェクトを選択
2. 「**↑**」ボタンで上に移動、「**↓**」ボタンで下に移動
3. 「実行」ボタンをクリックすると、上から順に処理されます

---

## ファイルパターンの詳細

### 処理対象パターン（ProcessName）

プロジェクト設定画面の「処理対象パターン」テキストボックスに、1行1パターンで入力します。

**形式**:
```
*.txt
*.dic
*.png
*.jpg
*.wav
ghost/master/*.txt
shell/master/*.png
```

**パターンの制限**:
- サブディレクトリの再帰検索は**不可**（1レベルのみ）
- `ghost/master/*.txt` ✓ OK
- `ghost/**/*.txt` ✗ NG（再帰検索はサポートされていません）

**よくあるパターン**:
- 辞書ファイルのみ: `*.txt`, `*.dic`（各行に1つずつ）
- すべてのファイル: `*.*`
- 特定のファイル名: `descript.txt`, `install.txt`
- サブディレクトリ指定: `ghost/master/*.txt`, `shell/master/*.png`

### 除外パターン（ExcludeName）

プロジェクト設定画面の「除外パターン」テキストボックスに、1行1パターンで入力します。

**例**:
```
*.bak
*.tmp
*.log
desktop.ini
Thumbs.db
profile.txt
ghost/master/satori_savedata.txt
```

**よくある除外パターン**:
- バックアップ: `*.bak`
- 一時ファイル: `*.tmp`, `*.log`
- システムファイル: `desktop.ini`, `Thumbs.db`
- 開発用ファイル: `*.psd`, `*.ai`

---

## トラブルシューティング

### FTP接続に失敗する

#### エラー: "530 Login authentication failed"

**原因**: ユーザー名またはパスワードが間違っている

**解決方法**:
1. プロジェクトを「編集」で開く
2. サーバー、ユーザーID、パスワードを確認
3. FTPサーバーの管理画面でユーザー名・パスワードを確認

#### エラー: "タイムアウトしました"

**原因1**: パッシブモードの設定が合っていない

**解決方法**:
1. プロジェクトを「編集」で開く
2. 「パッシブモード」のチェックを切り替えてみる

**原因2**: ファイアウォールでブロックされている

**解決方法**:
- Windowsファイアウォールで`GhostDist.exe`を許可
- セキュリティソフトの設定を確認

#### エラー: "指定されたパスが見つかりません"

**原因**: FTPサーバー上のディレクトリが存在しない

**解決方法**:
- FTPクライアントで手動接続し、ディレクトリを作成
- または「アップロード先ディレクトリ」の設定を確認

---

### ファイルがアップロードされない

#### 原因1: 処理対象パターンに一致していない

**解決方法**:
1. プロジェクトを「編集」で開く
2. 「プレビュー」ボタンで実際に処理されるファイルを確認
3. 処理対象パターンを修正

#### 原因2: 除外パターンで除外されている

**解決方法**:
1. プロジェクトを「編集」で開く
2. 「プレビュー」ボタンで確認
3. ログウィンドウで「除外: ファイル名」と表示されていないか確認
4. 除外パターンを修正

#### 原因3: ファイルが変更されていない（ネットワーク更新の場合）

**解決方法**:
- ネットワーク更新では、MD5ハッシュが変わっていないファイルはアップロードされません
- ファイルを編集して保存し直す

---

### 日本語ファイル名が文字化けする

#### 原因: Shift_JISで表現できない文字が含まれている

**解決方法**:
- GhostDistは自動的にUnicode対応を追加します（Info-ZIP Unicode Path Extra Field）
- **SSP 2.5.55以降**を使用していれば正しく表示されます
- 旧版SSP（2.5.54以前）では文字化けする可能性があります

---

### HTMLの変数が置換されない

#### 原因1: HTMLパスが間違っている

**解決方法**:
1. プロジェクトを「編集」で開く
2. HTMLファイルのパスが正しいか、ファイルが存在するか確認

#### 原因2: 変数の記述が間違っている

**解決方法**:
- `%uploaddate%`のように、`%`で囲む
- スペルミスがないか確認

#### 原因3: 処理タイプがUploadではない

**解決方法**:
1. プロジェクトを「編集」で開く
2. 処理タイプが「ゴーストアーカイブ(NAR)作成/FTPアップロード」になっているか確認

---

## FAQ

### Q1: 複数のゴーストを同時に更新できますか？

**A**: はい、できます。

複数のプロジェクトにチェックを入れて「実行」ボタンをクリックすると、上から順番に処理されます。

### Q2: FTPサーバーを使わず、ローカルにNARファイルだけ作りたい

**A**: 「NAR作成のみ」プロジェクトを作成してください。

1. 「新規作成」ボタンをクリック
2. 処理タイプで「ゴーストアーカイブ(NAR)作成」を選択
3. 基準フォルダ、NARファイル名を設定
4. FTP設定は不要（自動的にグレーアウトされます）

NARファイルは基準フォルダと同じ場所に作成されます。

### Q3: ログファイルはどこに保存されますか？

**A**: `GhostDist.exe`と同じフォルダの`log`サブフォルダ内に`log<YYYYMMDDHHMMSS>.log`という形式で保存されます。

例: `C:\Tools\GhostDist\log\log20260114093045.log`

メイン画面の「ログファイルに保存」にチェックが入っている場合のみ保存されます。

### Q4: パスワードを平文で保存したくない

**A**: 現在のバージョンでは、パスワードは平文で保存されます。

設定ファイル（`ghostdist.ini`）のファイルアクセス権限を適切に設定してください。

### Q5: SSHFTPやSFTPに対応していますか？

**A**: いいえ、対応していません。

現在は通常のFTPとFTPS（Explicit TLS）のみ対応しています。

### Q6: Macで使えますか？

**A**: いいえ、Windows専用です。

.NET Frameworkに依存しているため、Windows以外では動作しません。

### Q7: 処理中にキャンセルできますか？

**A**: いいえ、現在のバージョンではキャンセル機能はありません。

処理が完了するまでお待ちください。

### Q8: プロキシ経由でFTP接続できますか？

**A**: いいえ、現在はプロキシに対応していません。

### Q9: ななろだアップロードとは何ですか？

**A**: ななろだ（NarNaLoader）は、ゴーストのnarファイル をアップロードすることで簡単にゴーストを公開・更新ができるシステムです。

**FTP直接アップロードとの違い**:
- **FTP**: 自分のサーバーにFTPでアップロード（サーバー管理もしくはレンタル契約が必要）
- **ななろだ**: 専用サーバーにHTTPでアップロード（サーバー不要、アカウント登録のみ）

**設定方法**:
1. プロジェクト設定画面で「ななろだアップロード」を選択
2. Ghost ID（ななろだで登録したゴーストID）を入力
3. アップロードURL（ななろだのアップロードスクリプトURL）を入力
4. ユーザーID・パスワードを入力

**注意**: HTMLファイルはローカルのみ更新され、FTPアップロードされません。

### Q10: NARファイルに絵文字や特殊文字を含むファイル名を使えますか？

**A**: はい、使えます。

GhostDistは**Info-ZIP Unicode Path Extra Field（0x7075）**を自動的に埋め込むため、Shift_JISで表現できない文字（絵文字、一部の記号等）もサポートされます。

**動作**:
- **SSP 2.5.55以降**: Unicode対応、絵文字等が正しく表示されます
- **SSP 2.5.54以前**: Shift_JISに変換され、`?`等に置換される可能性があります

**ログ**: Unicode対応が必要なファイルは、ログに「Unicode対応: ファイル名」と表示されます。

### Q11: 共通FTP設定はどこで確認・変更できますか？

**A**: 共通FTP設定は以下の方法で確認・変更できます。

1. 任意のプロジェクトを「編集」で開く
2. FTP設定セクションでサーバー情報を確認
3. 「設定として保存」ボタンをクリックすると、共通FTP設定として上書き保存されます

**注意**: 共通FTP設定を使用しているすべてのプロジェクトに影響します。

### Q12: デフォルトパターンは何が含まれていますか？

**A**: プロジェクト設定画面で処理対象パターン・除外パターンを空欄にすると、デフォルトパターンが自動適用されます。

詳細は「ファイルパターンの詳細」セクションを参照してください。

---

## サポート

バグ報告や機能要望は、[GitHubのIssues](https://github.com/ukatech/ghostdist/issues)でお願いします。

